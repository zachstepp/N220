const pi = Math.PI; //pi is a constant used in math, using a shortcut here so I don't have to write the funciton every time
const radius = 5; //radius probably won't change so using a const
let area = pi * radius ** 2; //area forumla, assigning the result
let perimeter = 2 * pi * radius; //perimeter formula, assigning the result
console.log("Area: ", area);
console.log("Perimeter: ", perimeter);

